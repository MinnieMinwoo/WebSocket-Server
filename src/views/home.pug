
doctype html
html(lang="en")
     head
        meta(charset="UTF-8")
        title Zoom
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
     body
        header
            h1 Zoom
        main
            video#myFace(autoplay, playsinline, width = 50, height = 50)
            //
                width = height = 400
            button#mute Mute
            button#camera Turn Camera Off
            select#cameras
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")

//
    doctype html
    html(lang="en")
        head
            meta(charset="UTF-8")
            title Zoom
            link(rel="stylesheet", href="https://unpkg.com/mvp.css")
        body 
            header
                h1 WebSocket Server
            main
                div#welcome
                    form
                        input#roomName(placeholder="room name", required, type="text")
                        input#firstName(placeholder="nickname", required, type="text")
                        button Enter Room
                    h4 Open Rooms:
                    ul
                div#room
                    h3
                    ul
                    form#name
                        input(placeholder="Nickname" required, type="text")
                        button Change
                    form#msg
                        input(placeholder="message", required, type="text")
                        button Send
            script(src="/socket.io/socket.io.js")
            script(src="/public/js/app.js")